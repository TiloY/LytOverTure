1. 创建一个本地创库  git init
.git 就是本地仓库  其中vue 是工作目录！  文件必须在工作目录之中

$ git init

$ git add .

$ git commit -m " the first commit vue"

$ ssh-keygen -t rsa

$ git remote add origin https://github.com/TiloY/LytOverTure.git

$ git push -u origin master
Username for 'https://github.com': TiloY
